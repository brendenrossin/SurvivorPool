{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python init_db_railway.py && echo '✅ DB Init Complete' && (python jobs/ingest_personal_sheets.py; echo '📊 Sheets ingestion attempted') && (python jobs/update_scores.py; echo '🏈 Scores update attempted') && echo '🚀 Starting Streamlit...' && streamlit run app/main.py --server.port $PORT --server.address 0.0.0.0"
  }
}